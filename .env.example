# Production Environment Variables

# Domain name for your deployment
DOMAIN=yourdomain.com

# Database password (generate a strong password)
DB_PASSWORD=your_secure_db_password

# Symfony app secret (run: php -r "echo bin2hex(random_bytes(16));")
APP_SECRET=your_app_secret_here

# JWT passphrase for token signing
JWT_PASSPHRASE=your_jwt_passphrase

# SMTP settings for email
MAILER_DSN=smtp://user:pass@smtp.example.com:587

###> knpuniversity/oauth2-client-bundle ###
# Google OAuth credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Enable/disable Google OAuth login (true/false)
GOOGLE_AUTH_ENABLED=true
# Restrict logins to specific email domain(s) - comma-separated (leave empty to allow all)
GOOGLE_ALLOWED_DOMAINS=
###< knpuniversity/oauth2-client-bundle ###
