---
all:
  hosts:
    production:
      ansible_host: your-server-ip
      ansible_user: root
      # Or use a sudo user:
      # ansible_user: deploy
      # ansible_become: yes

    # Optional: staging server
    # staging:
    #   ansible_host: staging-server-ip
    #   ansible_user: root
